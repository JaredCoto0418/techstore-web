# URL del servidor API (opcional en desarrollo con proxy de Vite)
# Si se omite, Vite redirige /api y /uploads a http://localhost:5182
# VITE_API_URL=https://localhost:7066

# Cliente ID de PayPal Sandbox (necesario para checkout)
# VITE_PAYPAL_CLIENT_ID=tu_client_id_de_sandbox
